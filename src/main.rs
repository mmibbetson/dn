use crate::filename::get_filename;
use crate::filename::FilenameSegment;

mod args;
mod config;
mod filename;
mod frontmatter;
mod filename_parse;

// Top-down draft using api
fn main() {
    // get_args();
    // get_config();
    // get_filename();
    // get_frontmatter();
    // get_template();
    // create_file();
}
